services:
  - type: web
    name: vincent-fastapi
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn profile_docs.api_server:app --host 0.0.0.0 --port 10000
    envVars:
      - key: OPENROUTER_API_KEY
        fromSecret: OPENROUTER_API_KEY
